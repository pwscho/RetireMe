<UserControl x:Class="RetireMe.UI.Views.RothConversionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Add Conversion"
                    Command="{Binding AddConversionCommand}"
                    Margin="0,0,8,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Conversions}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}" Width="*"/>

                <DataGridComboBoxColumn Header="Owner"
                        SelectedValueBinding="{Binding OwnerId, Mode=TwoWay}"
                        SelectedValuePath="Id"
                        DisplayMemberPath="Name"
                        Width="120">

                    <DataGridComboBoxColumn.ElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource"
                    Value="{Binding DataContext.OwnerOptions,
                                    RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                            <Setter Property="IsHitTestVisible" Value="False"/>
                            <Setter Property="Focusable" Value="False"/>
                        </Style>
                    </DataGridComboBoxColumn.ElementStyle>

                    <DataGridComboBoxColumn.EditingElementStyle>
                        <Style TargetType="ComboBox">
                            <Setter Property="ItemsSource"
                    Value="{Binding DataContext.OwnerOptions,
                                    RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                        </Style>
                    </DataGridComboBoxColumn.EditingElementStyle>

                </DataGridComboBoxColumn>


                <DataGridTextColumn Header="Start Age"
                                    Binding="{Binding StartAge}" Width="80"/>

                <DataGridTextColumn Header="End Age"
                                    Binding="{Binding EndAge}" Width="80"/>

                <DataGridTextColumn Header="Annual Amount"
                                    Binding="{Binding AnnualAmount, StringFormat=C0}" Width="120"/>

                <!-- Owner selection will be wired later (combo of Primary/Spouse) -->
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

